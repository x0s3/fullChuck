import React from 'react';
import { SafeAreaView, Text } from 'react-native';

export const App = React.memo(() => (
  <SafeAreaView>
    <Text>FullChuck MonoRepo Typescript App :)</Text>
  </SafeAreaView>
));
